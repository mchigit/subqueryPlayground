{
  "name": "subQLPlayground",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -b",
    "prepack": "rm -rf dist && npm build",
    "test": "jest",
    "codegen": "./node_modules/.bin/subql codegen"
  },
  "homepage": "https://github.com/subquery/subql-starter",
  "repository": "github:subquery/subql-starter",
  "files": [
    "dist",
    "schema.graphql",
    "project.yaml"
  ],
  "author": "mchi",
  "license": "MIT",
  "devDependencies": {
    "@polkadot/api": "^6",
    "@subql/cli": "latest",
    "typescript": "^4.1.3"
  },
  "dependencies": {
    "@subql/contract-processors": "^0.4.0",
    "@subql/types": "latest",
    "ethers": "^5.5.3"
  }
}
